{
  "name": "studlyf-root",
  "private": true,
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
