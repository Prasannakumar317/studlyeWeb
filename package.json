{
  "name": "studlyf-root",
  "private": true,
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build": "npm exec --prefix frontend vite build"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
